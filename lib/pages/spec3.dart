import 'package:flutter/material.dart';

class spec3 extends StatelessWidget {
  const spec3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
